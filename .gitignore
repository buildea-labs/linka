# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr

# Env files: nunca commitar (nem .env, nem .env.production, nem *.local)
.env
.env.*
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code worktrees (embedded git repos — não commitar)
.claude/worktrees/

# Claude Code lockfiles do harness (runtime — não versionar)
.claude/*.lock

# Capacitor / Android toolchain local (não versionado)
_android-toolchain/

# APKs gerados para entrega manual (não versionar binários)
builds/apk/*.apk

# Xcode User State
xcuserdata/
*.xcuserdatad
*.xcuserstate

# Swift Package Manager build cache (por pacote, em aplicativo-ios/*/.build/)
.build/


# Firebase Hosting deploy cache
.firebase/

# Build Derived
build_derived/


# SwiftPM
.swiftpm/

