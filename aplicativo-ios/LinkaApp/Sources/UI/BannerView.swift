import SwiftUI

/// Ads ficam fora da build de producao ate existir configuracao oficial.
/// A view permanece como ponto de extensao sem reservar espaco na interface.
struct BannerView: View {
    var body: some View {
        EmptyView()
    }
}
