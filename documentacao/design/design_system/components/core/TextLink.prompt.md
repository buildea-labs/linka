# TextLink

Mono forward-link ("Abrir app →", "Ver protótipo →") used as the closing action inside cards.
Distinct from `Button`: it navigates, it never triggers a measurement.

```jsx
<TextLink href="Linka SpeedTest.html">Abrir app</TextLink>
```

Hover is the warm accent — the same rule as body links in the prototype pages.
