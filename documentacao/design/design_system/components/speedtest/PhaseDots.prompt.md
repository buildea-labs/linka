Small mono-caps step indicator under the measuring ring — dot + label per phase, muted/done/current color states, no lines connecting them.

```jsx
<PhaseDots phases={[{key:'download',label:'Download'},{key:'upload',label:'Upload'}]} activeKey="upload" />
```
